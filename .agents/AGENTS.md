# Project Rules

- **Git Push Policy**: Do NOT automatically push changes to remote (GitHub) after individual code edits or bug fixes. Accumulate updates and bug fixes locally first, and only push when explicitly asked by the user or when a substantial batch of updates and fixes is complete.
